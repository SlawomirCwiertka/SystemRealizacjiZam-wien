@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            
            <!--Html.ActionLink("Zarządzaj", "AdminManage", "Account", routeValues: null, htmlAttributes: new { title = "Manage" })&nbsp;&nbsp;&nbsp;-->
        </li>
     
        <li><a href="javascript:document.getElementById('logoutForm').submit()"></a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <!--<li>@Html.ActionLink("Rejestracja", "Registration", "Registration", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>-->
        <li>@Html.ActionLink("Zaloguj", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
